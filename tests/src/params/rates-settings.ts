import {RatesInitSettings} from "../interfaces/wrappers"

export const defaultRatesSettings: RatesInitSettings = {
    rate_timeout: 120,
    query_timeout: 60,
    provablecb1a_interval: 60,
    delphioracle_interval: 15,
    equilibriumdsp_interval: 15,
}
